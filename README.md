# tflm_tree-model_switch

## Environment

ubuntu 20.04

```
sudo apt install gcc g++ make cmake
```

## Example

```
cd tflm_tree-model_switch
mkdir build && cd build
cmake ..
make

cd src
./ModelChange
```